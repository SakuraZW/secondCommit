#include "include.h"

int main()
{
  ftm_pwm_init(FTM0, FTM_CH0, 20000, 200);
	  ftm_pwm_init(FTM0, FTM_CH1, 20000, 0);
          while(1);
          while(1);
          while(1);
          while(1);
          while(1);
          while(1);
	
while(1);
}
